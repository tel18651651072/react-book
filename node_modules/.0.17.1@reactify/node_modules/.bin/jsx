#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../.0.12.2@react-tools/bin/jsx" "$@"
  ret=$?
else 
  node  "$basedir/../../../.0.12.2@react-tools/bin/jsx" "$@"
  ret=$?
fi
exit $ret
