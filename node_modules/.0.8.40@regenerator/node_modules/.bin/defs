#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../.1.1.1@defs/build/es5/defs" "$@"
  ret=$?
else 
  node  "$basedir/../../../.1.1.1@defs/build/es5/defs" "$@"
  ret=$?
fi
exit $ret
